# Files that aren't in git but still need to reach every new worktree.
.env

# Nothing port-related belongs here: docker-compose.worktree.yml is committed,
# so it reaches every worktree through git.
