# Copy to .env. The file is not in git — .worktreeinclude is what gets it copied
# into every new worktree, so the environment comes up complete on the first try.
APP_GREETING=hello from the worktree
